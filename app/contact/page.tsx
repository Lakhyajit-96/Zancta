import { Navigation, Footer } from "@/components/marketing/nav";
export const metadata = { title: "Contact — LocalFile" };
export default function Contact(){return(<><Navigation /><main className="mx-auto max-w-3xl px-6 py-12"><h1 className="text-3xl font-semibold">Contact</h1><p className="mt-3 text-sm text-muted-foreground">Contact via email — no backend form at MVP. support@localfile.example (placeholder).</p></main><Footer /></>)}
