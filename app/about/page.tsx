import { Navigation, Footer } from "@/components/marketing/nav";
export const metadata = { title: "About — LocalFile" };
export default function About(){return(<><Navigation /><main className="mx-auto max-w-3xl px-6 py-12"><h1 className="text-3xl font-semibold">About</h1><p className="mt-3 text-sm text-muted-foreground">LocalFile is a privacy-first toolkit — files are processed locally in your browser. We don&apos;t chase trends; we solve the upload anxiety that existing suites ignore.</p></main><Footer /></>)}
