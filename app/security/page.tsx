import { Navigation, Footer } from "@/components/marketing/nav";
export const metadata = { title: "Security — LocalFile" };
export default function Security(){return(<><Navigation /><main className="mx-auto max-w-3xl px-6 py-12"><h1 className="text-3xl font-semibold">Security</h1><p className="mt-3 text-sm text-muted-foreground">Processing in Workers, CSP headers, file validation before processing, no execution of embedded PDF JS.</p><p className="mt-2 text-xs text-muted-foreground">Report: security@localfile.example (placeholder).</p></main><Footer /></>)}
