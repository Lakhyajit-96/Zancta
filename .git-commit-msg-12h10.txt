feat(growth): analytics foundation, event contract, growth dashboard, SEO improvements

Phase 12H-10: Build the complete growth and revenue foundation.

Analytics architecture:
- Create typed event taxonomy with 18 events across acquisition, tool,
  auth, and monetization categories (lib/analytics/events.ts)
- Implement consent-gated client tracker wrapping GA4 (lib/analytics/tracker.ts)
- Add server-side product event recording to AuditEvent (lib/analytics/server-events.ts)
- Fix tracking gap: OCR and PDF Text Extractor now fire tool_used,
  processing_started, processing_completed, and download_completed events
- Add processing_started, processing_failed, processing_cancelled,
  download_completed events to GenericToolShell
- Wire server-side subscription_active, subscription_cancelled,
  payment_failed, refund_completed events from billing webhooks
- Wire server-side signup_completed and checkout_started events
- Add tool_view, pricing_view, tool_catalog_view tracking via TrackView component

Growth dashboard:
- Admin-only /admin/growth route with ADMIN entitlement gating
- Displays: users, conversion funnel, revenue, entitlements, subscriptions,
  checkouts, audit activity, and recent signups — all from live database
- Protected by auth + ADMIN plan check + noindex + robots disallow

SEO improvements:
- Update all 11 tool SEO titles and descriptions for search intent
  alignment (include "online", "free", "no upload", format keywords)
- Add priority and changeFrequency to sitemap entries
- Add /admin to robots.txt disallow list

Observability:
- Structured error categorization system (lib/observability/errors.ts)
- Category-based error logging with severity levels

Testing:
- 28 new tests: analytics contract validation, param sanitization,
  privacy safety, SEO safety, route coverage (all pass)
- Full suite: 239 tests pass, zero regressions
- Typecheck: clean
- Lint: zero new errors

Privacy verification:
- Analytics never captures file contents, filenames, OCR text, or PII
- Param sanitization rejects path traversal and arbitrary input
- All tracking is consent-gated (GA4) or server-side product events only
- PAYMENTS_LIVE_ENABLED remains false
